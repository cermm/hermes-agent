--- a/plugins/kanban_completion_dispatch_wake/__init__.py
+++ b/plugins/kanban_completion_dispatch_wake/__init__.py
@@ -727,8 +727,11 @@
 def _install_recent_success_override() -> None:
     try:
-        from hermes_cli import kanban_db
+        from hermes_cli import kanban_db_dispatch as kanban_db
     except ImportError:
-        return
+        try:
+            from hermes_cli import kanban_db
+        except ImportError:
+            return
     current = kanban_db.check_respawn_guard
     if getattr(current, "_issue_413_semantic_return_override", False):
         return
